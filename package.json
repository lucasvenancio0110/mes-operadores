{
  "name": "mes-operadores",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply mes-operadores-db --remote",
    "build:factory-map-pixi": "esbuild app/factory-map-pixi-source.js --bundle --format=esm --platform=browser --target=es2022 --minify --outfile=app/vendor/factory-map-pixi.bundle.js"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "pixi.js": "8.19.0",
    "pixi-viewport": "6.0.3",
    "wrangler": "^4.68.0"
  }
}
