{
  "name": "mes-operadores",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply mes-operadores-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
