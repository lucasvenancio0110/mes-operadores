name: Apply NEOMES authenticated boot resilience fix

on:
  push:
    branches:
      - agent/neomes-architecture-recovery
    paths:
      - scripts/apply-auth-boot-resilience-fix.mjs
      - .github/workflows/apply-neomes-auth-boot-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout recovery branch
        uses: actions/checkout@v4
        with:
          ref: agent/neomes-architecture-recovery

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply deterministic patch
        run: node scripts/apply-auth-boot-resilience-fix.mjs

      - name: Validate patched modules
        run: |
          node --check app/auth-shell.js
          node --check app/operator-main.js

      - name: Commit patched runtime
        run: |
          if git diff --quiet -- app/auth-shell.js app/operator-main.js; then
            echo "No runtime changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/auth-shell.js app/operator-main.js
          git commit -m "fix: isolate authenticated operational module boot"
          git push origin HEAD:agent/neomes-architecture-recovery
